[pytest]
markers =
    portal_build: full npm ci + astro build + live-server boot smoke test for the
        portal (slow -- does a real Node install and Astro build). Excluded from
        the default run; CI runs it explicitly with `-m portal_build`.
addopts = -m "not portal_build"
