---
name: merge
description: Deprecated alias for /jMerge. Prints the new name once, then behaves exactly as /jMerge.
---
# /merge is now /jMerge

Tell the user once, in one line: "/merge is now /jMerge and this alias will be removed in the next release."

Then read `skills/jMerge/SKILL.md` and follow it exactly as if the user had typed `/jMerge` with the same arguments.
