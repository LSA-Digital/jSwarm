---
name: fix
description: Deprecated alias for /jFix. Prints the new name once, then behaves exactly as /jFix.
---
# /fix is now /jFix

Tell the user once, in one line: "/fix is now /jFix and this alias will be removed in the next release."

Then read `skills/jFix/SKILL.md` and follow it exactly as if the user had typed `/jFix` with the same arguments.
