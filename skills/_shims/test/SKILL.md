---
name: test
description: Deprecated alias for /jTest. Prints the new name once, then behaves exactly as /jTest.
---
# /test is now /jTest

Tell the user once, in one line: "/test is now /jTest and this alias will be removed in the next release."

Then read `skills/jTest/SKILL.md` and follow it exactly as if the user had typed `/jTest` with the same arguments.
