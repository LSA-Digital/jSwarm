---
name: jsetup
description: Deprecated alias for /jSetup. Prints the new name once, then behaves exactly as /jSetup.
---
# /jsetup is now /jSetup

Tell the user once, in one line: "/jsetup is now /jSetup and this alias will be removed in the next release."

Then read `skills/jSetup/SKILL.md` and follow it exactly as if the user had typed `/jSetup` with the same arguments.
